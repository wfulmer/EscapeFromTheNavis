/// @description set instance variables

spawn_active = true;//so it will only spawn enemies once
tot_enemy = 4;//total enemies it will spawn
reward = false;//so it will only reward the player once
r_chance = irandom_range(0,5);//the chance that it will spawn items